<Page
    x:Class="DengueApp.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DengueApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>

        <Style x:Key="StyleHomeButtonPrincipal" TargetType="Button">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="70" />
            <Setter Property="RenderTransformOrigin" Value="0.293,1.18" />
            <Setter Property="Foreground" Value="#FF474444" />
            <Setter Property="Width" Value="330" />
            <Setter Property="FontSize" Value="18" />
        </Style>


    </Page.Resources>

    <Grid Background="{ThemeResource AppBarItemForegroundThemeBrush}">
        <StackPanel x:Name="stackPanel" HorizontalAlignment="Left" Height="292" VerticalAlignment="Top" Width="330" Margin="0,250,0,0" RenderTransformOrigin="0.5,0.5">

            <StackPanel.Resources>

                <Style x:Key="StyleHomeButton" TargetType="Button">
                    <Setter Property="HorizontalAlignment" Value="Left"></Setter>
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="Background" Value="{x:Null}" />
                    <Setter Property="BorderBrush" Value="#FFBB0000" />
                    <Setter Property="Foreground" Value="#FFFB0000" />
                    <Setter Property="FontSize" Value="18" />
                </Style>
                
            </StackPanel.Resources>
            <StackPanel.RenderTransform>
                <CompositeTransform/>
                </StackPanel.RenderTransform>
            <Button x:ConnectionId='1' x:Name="buton1"                             HorizontalContentAlignment="Stretch" Style="{StaticResource StyleHomeButton}">
                <TextBlock Text="Sintomas" TextAlignment="Left"/>
            </Button>
            <Button x:ConnectionId='2' x:Name="buton2"                             HorizontalContentAlignment="Stretch" Style="{StaticResource StyleHomeButton}">
                <TextBlock Text="Atividades de Prevenção" TextAlignment="Left"/>
            </Button>
            <Button x:ConnectionId='3' x:Name="buton3"                             HorizontalContentAlignment="Stretch" Style="{StaticResource StyleHomeButton}">
                <TextBlock Text="Medicamentos" TextAlignment="Left"/>
            </Button>
            <Button x:ConnectionId='4' x:Name="buton4"                             HorizontalContentAlignment="Stretch" Style="{StaticResource StyleHomeButton}">
                <TextBlock Text="Outro topico blablabla" TextAlignment="Left"/>
            </Button>
            </StackPanel>

        <StackPanel HorizontalAlignment="Left" Height="106" Margin="0,161,0,0" VerticalAlignment="Top" Width="547">
            <Button x:Name="btAtividades" Style="{StaticResource StyleHomeButtonPrincipal}"   />

        </StackPanel>



    </Grid>


</Page>

