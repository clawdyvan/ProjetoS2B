<Page
    x:Class="DengueApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DengueApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid Background="{ThemeResource AppBarItemForegroundThemeBrush}">

        <Hub Margin="0, 50, 0, 0">
            
            <HubSection Header="Home" Foreground="red">
                <DataTemplate>
                    <Grid x:Name="section1" Loaded="HubSectionLoaded"/>
                </DataTemplate>
            </HubSection>

            <HubSection Header="Atividades" Foreground="red">
                <DataTemplate>
                    <Grid x:Name="section2"  Loaded="HubSectionLoaded"/>
                </DataTemplate>
            </HubSection>
            
        </Hub>
    </Grid>
</Page>